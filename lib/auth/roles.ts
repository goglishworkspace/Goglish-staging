/** All roles that land in the single /admin area - shared between the admin
 * layout's role-gate and resolveOwnDashboardPath() so the two never drift. */
export const STAFF_ROLES = ["admin", "super_admin", "moderator", "support", "content_manager", "accountant"];
